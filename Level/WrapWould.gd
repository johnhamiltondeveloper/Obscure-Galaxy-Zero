extends Spatial

#var Border = 35

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass

#func _process(delta):
	
#	var size = get_viewport().size.normalized()
	
	#for i in self.get_children():
		
	#	if Border*size.y < i.transform.origin.z or -Border*size.y > i.transform.origin.z:
	#		i.transform.origin.z = -i.transform.origin.z*0.97
			
	#	if Border*size.x < i.transform.origin.x or -Border*size.x > i.transform.origin.x:
	#		i.transform.origin.x = -i.transform.origin.x*0.97
